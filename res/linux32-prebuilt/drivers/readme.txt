Los drivers no son necesarios en Linux.
Si la placa no es detectada asegurese que su usuario tiene acceso al puerto serie.
De no tenerlo puede dárselos añadiendo al usuario al grupo dialout con el comando:

sudo adduser <username> dialout

